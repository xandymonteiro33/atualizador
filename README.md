# atualizador

A simple project demonstrating scraping utilities.

## Running Tests

This project uses [pytest](https://pytest.org/) for unit testing. To run
the tests, install the development requirements and execute `pytest`:

```bash
pip install pytest
pytest
```
